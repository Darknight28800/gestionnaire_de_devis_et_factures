# URL du backend (API Express). En local, laissez vide pour utiliser http://localhost:4000.
# En production (ex : frontend sur Vercel, backend sur Railway), mettez l'URL publique du backend.
VITE_API_URL=
